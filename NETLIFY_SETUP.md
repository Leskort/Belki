# 🚀 Пошаговая инструкция по деплою на Netlify

## Шаг 1: Настройка базы данных Turso

1. Зайдите на https://turso.tech
2. Войдите через GitHub или создайте аккаунт
3. Создайте базу данных:
   - Нажмите "Create Database"
   - Назовите её (например, `elki-db`)
   - Выберите регион (например, `us-east-1`)
4. После создания:
   - Скопируйте Connection String (например: `libsql://elki-db-username.aws-us-east-1.turso.io`)
5. Создайте токен доступа:
   - Перейдите в Settings → Tokens
   - Нажмите "Create Token"
   - Скопируйте токен (показывается только один раз!)
6. Сформируйте полный connection string:
   ```
   libsql://elki-db-username.aws-us-east-1.turso.io?authToken=ваш-токен
   ```

## Шаг 2: Обновление connection string в коде

Откройте файл `lib/prisma.ts` и замените строку 10:

**Было:**
```typescript
const DEFAULT_DATABASE_URL = process.env.DATABASE_URL || 
  'libsql://elki-db-YOUR-USERNAME.aws-us-east-1.turso.io?authToken=YOUR-AUTH-TOKEN'
```

**Стало:**
```typescript
const DEFAULT_DATABASE_URL = process.env.DATABASE_URL || 
  'libsql://elki-db-username.aws-us-east-1.turso.io?authToken=ваш-реальный-токен'
```

## Шаг 3: Загрузка кода на GitHub

1. Создайте новый репозиторий на GitHub
2. Выполните команды в терминале:

```bash
# Инициализация git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: Elki Horror Shop"

# Добавление remote репозитория (замените YOUR-USERNAME и YOUR-REPO)
git remote add origin https://github.com/YOUR-USERNAME/YOUR-REPO.git

# Отправка на GitHub
git branch -M main
git push -u origin main
```

## Шаг 4: Подключение к Netlify

### Вариант A: Через веб-интерфейс (рекомендуется)

1. Зайдите на https://app.netlify.com
2. Войдите через GitHub
3. Нажмите "Add new site" → "Import an existing project"
4. Выберите ваш GitHub репозиторий
5. Настройки сборки (Netlify обычно определяет автоматически):
   - **Build command**: `npm run build`
   - **Publish directory**: `.next`
   - **Base directory**: (оставьте пустым)
6. Нажмите "Deploy site"

### Вариант B: Через Netlify CLI

```bash
# Установка Netlify CLI (если еще не установлен)
npm install -g netlify-cli

# Вход в Netlify
netlify login

# Инициализация проекта
netlify init

# Следуйте инструкциям:
# - Выберите "Create & configure a new site"
# - Выберите команду сборки: npm run build
# - Выберите директорию публикации: .next
# - Настройте GitHub деплои (опционально)

# Деплой на продакшен
netlify deploy --prod
```

## Шаг 5: Настройка переменных окружения (опционально, но рекомендуется)

Вместо встраивания connection string в код, можно использовать переменные окружения:

1. В Netlify: Site settings → Environment variables → Add variable
2. Добавьте переменные:

   - **DATABASE_URL**
     - Значение: `libsql://elki-db-username.aws-us-east-1.turso.io?authToken=ваш-токен`
   
   - **NEXTAUTH_SECRET**
     - Значение: сгенерируйте случайную строку (минимум 32 символа)
     - Можно сгенерировать: `openssl rand -base64 32`
   
   - **NEXTAUTH_URL**
     - Значение: URL вашего сайта на Netlify
     - Например: `https://your-site-name.netlify.app`

3. После добавления переменных, пересоберите сайт:
   - Site overview → Deploys → Trigger deploy → Deploy site

## Шаг 6: Выполнение миграций базы данных

После первого деплоя нужно выполнить миграции:

### Вариант A: Локально с production connection string

```bash
# Установите переменную окружения с вашим connection string
export DATABASE_URL="libsql://elki-db-username.aws-us-east-1.turso.io?authToken=ваш-токен"

# Выполните миграции
npm run db:migrate:deploy

# Заполните базу данных
npm run db:seed
```

### Вариант B: Через Netlify Functions (если нужно)

Можно создать функцию для миграций, но проще сделать локально.

## Шаг 7: Проверка работы сайта

1. Откройте ваш сайт на Netlify (URL будет вида `https://your-site-name.netlify.app`)
2. Проверьте главную страницу
3. Проверьте каталог товаров
4. Войдите в админ-панель:
   - URL: `https://your-site-name.netlify.app/admin/login`
   - Email: `admin@elki.by`
   - Пароль: `admin123`
5. Проверьте создание заказа

## ⚠️ Важные моменты

- ✅ База данных работает в облаке (Turso) - не требует настройки на Netlify
- ✅ Connection string можно встроить в код ИЛИ использовать env переменные
- ✅ Все заказы сохраняются в БД
- ✅ Админ-панель для управления товарами
- ⚠️ **ОБЯЗАТЕЛЬНО смените пароль админа после первого входа!**

## 🔧 Troubleshooting

### Ошибка "Database connection error"

- Проверьте connection string в `lib/prisma.ts` или в переменных окружения Netlify
- Убедитесь, что токен Turso действителен
- Проверьте, что база данных создана в Turso

### Ошибка сборки на Netlify

- Проверьте логи сборки в Netlify (Deploys → выберите деплой → View build log)
- Убедитесь, что `package.json` содержит все зависимости
- Проверьте, что `netlify.toml` настроен правильно

### Ошибка аутентификации

- Проверьте `NEXTAUTH_SECRET` в переменных окружения
- Убедитесь, что `NEXTAUTH_URL` указан правильно (должен быть полный URL вашего сайта)

### Миграции не выполняются

- Выполните миграции локально с production connection string
- Или создайте отдельную функцию для миграций

## 📝 Чек-лист перед деплоем

- [ ] База данных Turso создана
- [ ] Connection string обновлен в `lib/prisma.ts`
- [ ] Код загружен на GitHub
- [ ] Сайт подключен к Netlify
- [ ] Переменные окружения настроены (опционально)
- [ ] Миграции выполнены
- [ ] Seed выполнен (создан админ)
- [ ] Сайт работает и доступен
- [ ] Админ-панель работает
- [ ] Пароль админа изменен

## 🎉 Готово!

После выполнения всех шагов ваш магазин будет работать на Netlify с облачной базой данных Turso!

