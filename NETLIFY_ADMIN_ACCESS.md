# Как зайти в админку на Netlify

## 📍 URL для входа

После деплоя на Netlify, откройте в браузере:

```
https://ваш-сайт.netlify.app/admin/login
```

Или альтернативный путь:
```
https://ваш-сайт.netlify.app/login
```

## 🔑 Учетные данные

- **Email**: `admin@elki.by`
- **Пароль**: `admin123`

## ⚠️ ВАЖНО: Перед первым входом

Перед тем как зайти в админку, нужно выполнить миграции и создать пользователя в базе данных.

### Шаг 1: Настройте переменные окружения в Netlify

1. Зайдите в панель Netlify: https://app.netlify.com
2. Выберите ваш сайт
3. Перейдите в **Site settings** → **Environment variables**
4. Добавьте следующие переменные:

   - **DATABASE_URL**
     - Значение: ваш PostgreSQL connection string от Neon
     - Формат: `postgresql://user:password@host/database?sslmode=require`
   
   - **NEXTAUTH_SECRET**
     - Значение: сгенерируйте случайную строку (минимум 32 символа)
     - Можно сгенерировать командой: `openssl rand -base64 32`
   
   - **NEXTAUTH_URL**
     - Значение: URL вашего сайта на Netlify
     - Например: `https://ваш-сайт.netlify.app`

5. После добавления переменных, пересоберите сайт:
   - **Deploys** → **Trigger deploy** → **Deploy site**

### Шаг 2: Выполните миграции и seed локально

Выполните эти команды **локально в терминале**, но они применятся к вашей production базе данных:

```bash
# Перейдите в директорию проекта
cd /home/bes/KHJ/Belki

# Установите DATABASE_URL (используйте тот же connection string, что в Netlify)
export DATABASE_URL="ваш-postgresql-connection-string-от-neon"

# Выполните миграции (создаст таблицы в базе)
npm run db:migrate:deploy

# Заполните базу данных (создаст пользователя-админа, категории, товары)
npm run db:seed
```

После выполнения этих команд:
- ✅ Таблицы будут созданы в базе данных
- ✅ Пользователь `admin@elki.by` с паролем `admin123` будет создан
- ✅ Категории и товары будут добавлены

### Шаг 3: Войдите в админку

1. Откройте в браузере: `https://ваш-сайт.netlify.app/admin/login`
2. Введите:
   - Email: `admin@elki.by`
   - Пароль: `admin123`
3. Нажмите "Войти"

## 🔧 Если не получается войти

### Ошибка "Неверный email или пароль"

1. **Проверьте, что пользователь создан:**
   ```bash
   # Локально, с production DATABASE_URL
   export DATABASE_URL="ваш-production-connection-string"
   npm run db:seed
   ```

2. **Проверьте переменные окружения в Netlify:**
   - Убедитесь, что `DATABASE_URL` правильный
   - Убедитесь, что `NEXTAUTH_SECRET` установлен
   - Убедитесь, что `NEXTAUTH_URL` указывает на ваш сайт

3. **Проверьте логи Netlify:**
   - **Deploys** → выберите последний деплой → **View build log**
   - Ищите ошибки подключения к базе данных

### Ошибка подключения к базе данных

- Проверьте, что `DATABASE_URL` в Netlify правильный
- Убедитесь, что база данных Neon доступна
- Проверьте, что connection string содержит `?sslmode=require`

## 📝 Чек-лист перед входом

- [ ] Переменные окружения установлены в Netlify (DATABASE_URL, NEXTAUTH_SECRET, NEXTAUTH_URL)
- [ ] Сайт пересобран после добавления переменных
- [ ] Миграции выполнены локально с production DATABASE_URL
- [ ] Seed выполнен локально (пользователь создан)
- [ ] База данных Neon доступна и работает

## 🔒 Безопасность

⚠️ **ВАЖНО**: После первого входа обязательно смените пароль админа!

Текущий пароль `admin123` - это демо-пароль, его нужно изменить на более безопасный.

