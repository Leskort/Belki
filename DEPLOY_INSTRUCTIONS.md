# 🚀 Инструкция по деплою на Netlify

## Шаг 1: Подготовка кода

Убедитесь, что:
- ✅ Connection string Turso обновлен в `lib/prisma.ts`
- ✅ Все изменения сохранены
- ✅ `.env` файл НЕ будет загружен на GitHub (он уже в .gitignore)

## Шаг 2: Загрузка на GitHub

### Если у вас еще нет репозитория:

1. Создайте новый репозиторий на GitHub:
   - Зайдите на https://github.com
   - Нажмите "New repository"
   - Назовите репозиторий (например, `elki-horror-shop`)
   - НЕ создавайте README, .gitignore или лицензию (они уже есть)
   - Нажмите "Create repository"

2. Выполните команды в терминале:

```powershell
# Инициализация git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: Elki Horror Shop"

# Добавление remote репозитория (замените YOUR-USERNAME и YOUR-REPO)
git remote add origin https://github.com/YOUR-USERNAME/YOUR-REPO.git

# Отправка на GitHub
git branch -M main
git push -u origin main
```

### Если репозиторий уже существует:

```powershell
git add .
git commit -m "Update project"
git push
```

## Шаг 3: Подключение к Netlify

### Вариант A: Через веб-интерфейс (рекомендуется)

1. Зайдите на https://app.netlify.com
2. Войдите через GitHub
3. Нажмите **"Add new site"** → **"Import an existing project"**
4. Выберите **"Deploy with GitHub"**
5. Авторизуйте Netlify доступ к GitHub
6. Выберите ваш репозиторий `elki-horror-shop`
7. Настройки сборки (Netlify обычно определяет автоматически):
   - **Build command**: `npm run build`
   - **Publish directory**: `.next`
   - **Base directory**: (оставьте пустым)
8. Нажмите **"Deploy site"**

### Вариант B: Через Netlify CLI

```powershell
# Установка Netlify CLI (если еще не установлен)
npm install -g netlify-cli

# Вход в Netlify
netlify login

# Инициализация проекта
netlify init

# Следуйте инструкциям:
# - Выберите "Create & configure a new site"
# - Выберите команду сборки: npm run build
# - Выберите директорию публикации: .next

# Деплой на продакшен
netlify deploy --prod
```

## Шаг 4: Настройка переменных окружения в Netlify

**ВАЖНО!** Это нужно сделать для работы базы данных Turso на продакшене.

1. В Netlify перейдите в ваш сайт
2. Откройте **Site settings** → **Environment variables** → **Add variable**
3. Добавьте следующие переменные:

   **DATABASE_URL**
   - Значение: `libsql://elki-db-leskort.aws-us-east-1.turso.io?authToken=eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJhIjoicnciLCJpYXQiOjE3NjM0NzgzMzEsImlkIjoiYjRlZmE0MzEtNjliMy00ZWQ1LWI3YWYtN2I4ZTJiNzNjMWEzIiwicmlkIjoiYjFmMjNhZmQtNzY0ZS00OGI0LWJhMWUtN2NiNWRhYTdmNzc3In0.c1yeI6t5pJyjMztKh_lpl-aRa7pCmBVKbaLiwR4nluwmFbzRuAAZ8bmRPRBtpMJqqNVL2GckbVlf0xpqWmisAg`
   - Scope: All scopes (Production, Deploy previews, Branch deploys)

   **NEXTAUTH_SECRET**
   - Значение: сгенерируйте случайную строку (минимум 32 символа)
   - Можно сгенерировать онлайн или через PowerShell:
     ```powershell
     -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})
     ```
   - Scope: All scopes

   **NEXTAUTH_URL**
   - Значение: URL вашего сайта на Netlify
   - Например: `https://your-site-name.netlify.app`
   - Scope: Production

4. После добавления переменных, пересоберите сайт:
   - **Site overview** → **Deploys** → **Trigger deploy** → **Deploy site**

## Шаг 5: Выполнение миграций на продакшене

После первого деплоя нужно выполнить миграции базы данных Turso.

### Вариант A: Локально с production connection string

```powershell
# Установите переменную окружения
$env:DATABASE_URL="libsql://elki-db-leskort.aws-us-east-1.turso.io?authToken=eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJhIjoicnciLCJpYXQiOjE3NjM0NzgzMzEsImlkIjoiYjRlZmE0MzEtNjliMy00ZWQ1LWI3YWYtN2I4ZTJiNzNjMWEzIiwicmlkIjoiYjFmMjNhZmQtNzY0ZS00OGI0LWJhMWUtN2NiNWRhYTdmNzc3In0.c1yeI6t5pJyjMztKh_lpl-aRa7pCmBVKbaLiwR4nluwmFbzRuAAZ8bmRPRBtpMJqqNVL2GckbVlf0xpqWmisAg"

# Создайте схему базы данных
npx prisma db push

# Заполните базу данных
npm run db:seed
```

### Вариант B: Через Prisma Studio (визуально)

```powershell
$env:DATABASE_URL="libsql://elki-db-leskort.aws-us-east-1.turso.io?authToken=ваш-токен"
npx prisma studio
```

Затем вручную создайте таблицы или используйте `db push`.

## Шаг 6: Проверка работы сайта

1. Откройте ваш сайт на Netlify (URL будет вида `https://your-site-name.netlify.app`)
2. Проверьте главную страницу
3. Проверьте каталог товаров (`/catalog`)
4. Войдите в админ-панель:
   - URL: `https://your-site-name.netlify.app/admin/login`
   - Email: `admin@elki.by`
   - Пароль: `admin123`
5. Проверьте создание заказа

## ⚠️ Важные моменты

- ✅ База данных Turso работает в облаке - не требует настройки на сервере
- ✅ Connection string уже встроен в `lib/prisma.ts`, но лучше использовать env переменные
- ✅ Все заказы сохраняются в БД
- ⚠️ **ОБЯЗАТЕЛЬНО смените пароль админа после первого входа!**

## 🔧 Troubleshooting

### Ошибка "Database connection error"

- Проверьте `DATABASE_URL` в переменных окружения Netlify
- Убедитесь, что токен Turso действителен
- Проверьте, что база данных создана в Turso

### Ошибка сборки на Netlify

- Проверьте логи сборки: **Deploys** → выберите деплой → **View build log**
- Убедитесь, что `package.json` содержит все зависимости
- Проверьте, что `netlify.toml` настроен правильно

### Ошибка аутентификации

- Проверьте `NEXTAUTH_SECRET` в переменных окружения
- Убедитесь, что `NEXTAUTH_URL` указан правильно (полный URL сайта)

### Миграции не выполняются

- Выполните миграции локально с production connection string
- Используйте `npx prisma db push` вместо `migrate`

## 📝 Чек-лист

- [ ] Код загружен на GitHub
- [ ] Сайт подключен к Netlify
- [ ] Переменные окружения настроены в Netlify
- [ ] Сайт успешно собран и задеплоен
- [ ] Миграции выполнены на продакшене
- [ ] Seed выполнен (создан админ)
- [ ] Сайт работает и доступен
- [ ] Админ-панель работает
- [ ] Пароль админа изменен

## 🎉 Готово!

После выполнения всех шагов ваш магазин будет работать на Netlify с облачной базой данных Turso!

