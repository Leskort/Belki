# Скопируйте этот файл в .env.local и заполните своими данными
# 
# DATABASE_URL - Connection string от Neon PostgreSQL
# Формат: postgresql://user:password@host/database?sslmode=require
# 
# Получите connection string в панели управления Neon:
# https://console.neon.tech -> ваш проект -> Connection Details

DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# NEXTAUTH_SECRET - Секретный ключ для NextAuth (минимум 32 символа)
# Сгенерируйте: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here-min-32-chars"

# NEXTAUTH_URL - URL вашего сайта (для локальной разработки)
NEXTAUTH_URL="http://localhost:3000"
